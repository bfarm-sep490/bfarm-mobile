import home from '@/translations/locales/en/home.json';
import profile from '@/translations/locales/en/profile.json';
import signIn from '@/translations/locales/en/sign-in.json';

const resources = {
  home,
  profile,
  signIn,
} as const;

export default resources;
